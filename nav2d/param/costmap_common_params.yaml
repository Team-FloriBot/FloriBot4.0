obstacle_range: 4.5       # The default maximum distance from the robot at which an obstacle will be inserted into the cost map in meters
raytrace_range: 5.0       # The default range in meters at which to raytrace out obstacles from the map using sensor data
max_obstacle_height: 0.5  # The maximum height of any obstacle to be inserted into the costmap in meters
transform_tolerance: 1.0  # [s] Specifies the delay in transform (tf) data that is tolerable
# footprint: [[0.40, 0.22], [0.4, -0.22], [-1.16, -0.22], [-1.16, 0.22]] # floribot (in base_link): 387 --> 400, 212.5 --> 220, 1.153 --> 1.160
# This more accurate footprint has been determined by reading the corresponding dynamic reconfigure variable
# while the angle of the articulated joint is zero and the dyn_footprint_node.py is running.
#footprint: [[0.3, -0.2], [-0.3, -0.2], [-0.3, 0.2], [0.3, 0.2]]
footprint: [[-0.383, 0.001], [-0.243, 0.168], [0.277, 0.168], [0.277, -0.168], [-0.243, -0.168], [-0.383, -0.001]]
